name: First Workflow
on: workflow_dispatch
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - 
                name:   "Print Hello"
                run:    echo "Hello Guys"
            -   
                name:   "Print goodbye"
                run:    echo "Good bye for now"